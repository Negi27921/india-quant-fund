# Cloud API deployment — Vercel serverless
# Supabase replaces DuckDB; pandas-ta replaced with manual TA calcs to stay under 500MB

# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
requests>=2.32.0

# Data (keep slim — no duckdb, no pandas-ta)
yfinance>=0.2.38
pandas>=2.2.0
numpy>=1.26.0

# Web scraping (FII/DII, NSE)
beautifulsoup4>=4.12.0
lxml>=5.2.0

# BSE scraping (Cloudflare bypass via Chrome TLS impersonation)
curl_cffi>=0.7.0

# Local LLM fallback (OLLAMA)
ollama>=0.3.0

# Headless browser (BSE session refresh fallback)
playwright>=1.40.0

# Supabase cloud database
supabase>=2.4.0

# LLM providers (openai SDK for agents/base.py fallback)
openai>=1.30.0

# PDF parsing (results pipeline)
pdfminer.six>=20221105
pypdf>=4.0.0
pdfplumber>=0.10.0

# Config / utils
tenacity>=8.2.0
pydantic>=2.7.0
python-dotenv>=1.0.0
loguru>=0.7.2
pytz>=2024.1
python-dateutil>=2.9.0
pyyaml>=6.0
